matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required
    - os: osx
      osx_image: xcode7.2

install:
  - ./.travis/install.${TRAVIS_OS_NAME}.sh

script:
  - levee test -v tests/
